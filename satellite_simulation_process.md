# 卫星通信系统仿真计算过程详解

---

## 1. 系统初始化阶段

### 1.1 参数加载与预处理

- **系统参数加载**: 从 `system_params.json` 文件加载物理层参数（如载波频率、带宽、EIRP 密度）和物理常数。
- **波长计算**: 根据公式 `λ = c/f` 计算载波波长，其中 `c` 是光速，`f` 是载波频率。
- **天线参数计算**:
  - 基于天线直径（例如 1.5 米）计算 `ka` 参数：`ka = 2π/λ × (天线直径/2)`，该参数用于后续计算波束增益衰减。
- **EIRP 计算**: `EIRP = EIRP密度 + 10 * log₁₀(带宽/1MHz)`，计算等效全向辐射功率。

### 1.2 卫星位置数据加载

- 从指定的文本文件读取所有卫星在 ECEF 坐标系下的位置数据。
- 数据格式为时间戳后跟 `(x, y, z)` 坐标。单位默认为公里（km），在加载时统一转换为米（m）。
- 系统支持按卫星索引进行筛选，只加载仿真所需的特定卫星数据。

### 1.3 地面拓扑加载

- **小区中心加载**: 从 JSON 文件读取目标区域（如 A1、A2）内所有地面小区的中心点 ECEF 坐标。
- **用户位置加载**: 读取每个小区内所有用户的 ECEF 坐标。
- **用户-小区映射**: 建立用户 ID 与所属小区 ID 的精确对应关系，为后续的资源分配做准备。

---

## 2. 快照仿真阶段（每个时间点）

此阶段处理每个时间快照（Snapshot）下的静态计算。

### 2.1 卫星-小区关联计算

- **距离计算**: 对每个地面小区，计算其中心点到所有在轨卫星的欧几里得距离。
- **最近关联**: 每个小区选择距离最近的一颗卫星作为其服务卫星。
- **关联矩阵构建**: 生成一个 `[小区数 x 卫星数]` 的矩阵，矩阵中每行仅有一个值为 1（代表服务卫星），其余为 0。

### 2.2 干扰卫星识别

- **星下点计算**: 将每颗卫星的 ECEF 坐标投影到地球表面，得到其星下点经纬度。
- **地表距离计算**: 使用大圆距离公式计算服务卫星与其他卫星星下点之间的地表距离。
- **干扰区域定义**: 以服务卫星的星下点为中心，定义一个固定半径（如 2000km）的区域，该区域内的其他卫星被视为潜在的星间干扰源。
- **可见性判断**: 对每个用户，计算其与潜在干扰卫星的仰角。只有仰角大于特定阈值（如 10 度）的卫星才被认为是有效的干扰源。
- **干扰关联矩阵构建**: 生成一个 `[用户数 x 卫星数]` 的矩阵，用于标记每个用户的服务卫星（值为 2）和星间干扰卫星（值为 1）。

### 2.3 噪声功率预计算

- **等效噪声温度计算**: `Teq = 10^((G_R - G/T)/10)`，其中 `G_R` 是接收增益，`G/T` 是品质因数。
- **噪声功率计算**: `N₀ = k × Teq × B`，其中 `k` 是玻尔兹曼常数，`B` 是系统带宽。

---

## 3. 时隙仿真阶段（每个时隙）

此阶段处理每个时隙（Timeslot）下的动态计算。

### 3.1 波束调度（Round-Robin）

- **调度策略**:
  - **全量服务**: 如果一颗卫星关联的小区数量不多于其波束数量（如 50 个），则该卫星在每个时隙同时服务所有关联的小区。
  - **轮询服务**: 如果关联小区数超过波束数，则采用轮询机制，每个时隙服务其中的一部分小区（如 50 个）。
- **轮询索引计算**: `start_idx = (时隙索引 × 波束数) % 总小区数`，确定当前时隙服务的起始小区。
- **调度矩阵生成**: 记录每个卫星在当前时隙激活的波束所指向的小区。

### 3.2 接收功率计算

- **离轴角 (α) 计算**:
  - 计算从卫星指向小区中心和用户位置的两个向量。
  - 通过这两个向量的点积和模长计算它们之间的夹角，即离轴角。
- **增益衰减 (ΔG) 计算**:
  - `x = ka × sin(α)`
  - `ΔG = 10×log₁₀(4 × (J₁(x)/x)²)`, 其中 `J₁` 是一阶贝塞尔函数。
- **路径损耗 (PL) 计算**:
  - **自由空间损耗**: `32.45 + 20×log₁₀(频率 × 距离)`
  - **其他损耗**: 包括固定的闪烁损耗和服从正态分布的阴影衰落。
  - **总路径损耗** = 自由空间损耗 + 闪烁损耗 + 阴影衰落
- **总接收功率 (P_rx)**: `P_rx = EIRP + ΔG + 接收天线增益 - PL`

### 3.3 干扰功率计算

- **星内干扰**: 来自同一服务卫星的其他活跃波束的干扰。通过累加这些干扰波束在用户位置产生的接收功率得到。
- **星间干扰**: 来自其他干扰卫星的所有活跃波束的干扰。通过累加这些干扰源在用户位置产生的接收功率得到。

### 3.4 SINR 计算

- **信号功率**: 即服务卫星在用户位置产生的接收功率。
- **总干扰功率**: 星内干扰 + 星间干扰。
- **SINR (信干噪比)**: `SINR = 信号功率 / (总干扰功率 + 噪声功率)`
- **单位转换**: `SINR_dB = 10×log₁₀(SINR)`

---

## 4. 资源分配阶段

### 4.1 Round-Robin (RR) 资源分配

- **资源块**: 每个小区内的总带宽被划分为固定数量的资源块（如 150 个）。
- **分配策略**: 采用简单的轮询方式，将资源块依次分配给小区内的用户。
- **用户带宽**: `用户带宽 = 分配到的资源块数 × 单个资源块的带宽`

### 4.2 比例公平 (PF) 资源分配

- **瞬时速率计算**: `instant_rate = 带宽 × log₂(1 + SINR)`
- **PF 指标计算**: `PF_metric = 瞬时速率 / 历史平均吞吐量`
- **分配过程**:
  1. 计算小区内所有用户的 PF 指标。
  2. 选择 PF 指标最高的用户，为其分配一个资源块。
  3. 更新该用户的瞬时速率（因分配的资源增多）和 PF 指标。
  4. 重复此过程，直到所有资源块分配完毕。
- **历史平均更新**: `新平均速率 = α × 旧平均速率 + (1-α) × 当前吞吐量`，其中 `α` 是平滑因子。

---

## 5. 吞吐量计算

### 5.1 频谱效率

- **计算公式**: `η = log₂(1 + SINR)` (bits/s/Hz)

### 5.2 吞吐量

- **计算公式**: `吞吐量 = 用户分配到的总带宽 × 频谱效率` (bits/s)
- 分别为 RR 和 PF 两种调度策略计算各自的吞吐量。

---

## 6. 结果记录与统计

- **SINR 表格**: 记录每个用户在每个时隙的 SINR 值（dB）。
- **详细数据表格**: 记录每个用户在每个时隙的详细仿真数据，包括接收功率、各类干扰、噪声、频谱效率和两种调度策略下的吞吐量。
- **性能统计**: 计算平均 SINR、最大/最小值等统计指标。

---

## 7. 并行处理机制

- **用户分组**: 为加速仿真，将所有用户分为多个组（如 8 组）。
- **并行计算**: 使用多进程（`ProcessPoolExecutor`）并行处理每个用户组的仿真计算。
- **结果合并**: 在所有进程计算完成后，将各组的结果合并，形成完整的仿真输出数据。
